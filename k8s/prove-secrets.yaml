apiVersion: v1
kind: Secret
metadata:
  name: prove-secrets
type: Opaque
stringData:
  MONGO_URI: mongodb://prove-mongo:27017/prove
  JWT_SECRET: "REPLACE_WITH_STRONG_SECRET"
  # Add other secrets here (SMTP creds, third-party keys)
