### Example environment variables for PROVE backend
# Copy this file to `.env` in the `backend/` folder and fill secrets before running.

MONGODB_URI=mongodb://mongodb:27017
MONGODB_DB_NAME=prove
JWT_SECRET=change-me-to-a-secure-32+ char string
APP_ENV=production
