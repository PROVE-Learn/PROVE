# PROVE Frontend

Local dev:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

The dev server proxies `/api` to `http://localhost:8000` by default.
